# for running the code with RStudio
*.pper
.Rproj.user/44E0F474/sources/prop/184E462B
/.Rproj.user
plantismash.json
.Rhistory 
# input files (large pathway TTLs downloaded from Zenodo — not committed)
input_ttl/*

# output TTL files are committed so they are archived with each GitHub release
# (files are small: plantismash.ttl ~650KB, mibig.ttl ~112KB)
# Exception: do NOT commit large intermediate files
output_ttl/*.json
# Claude Code guidance file
CLAUDE.md